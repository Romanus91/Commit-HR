import React from 'react';
import { Box } from '@components/common/ui-kit';
import styles from './styles.module.less';

export const CandidateOverview: React.FC = () => {
    return <Box className={styles.container}>CandidateOverview</Box>;
};
